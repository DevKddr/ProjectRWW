Convert_wepons_to_json.py 설명서
WeaponsData.xlsx 파일을 Json 파일로 변환해주는 도구

-사용방법-

1. Cmd 실행
2. 원하는 파일 경로 지정
( cd ( 주소입력 ) )
3. 아래 명령어 실행
python convert_to_json.py RandomBox.xlsx WeaponData.xlsx ./output

하위 경로에 output 폴더에 파일 생성